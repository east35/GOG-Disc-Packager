<Window x:Class="GogDisc.Launcher.CollectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GOG Game Collection" Width="560" Height="620" MinHeight="440"
        Background="#F4F4F4" Foreground="#111111" FontFamily="Segoe UI"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="CollectionTitle" FontSize="28" FontWeight="SemiBold" TextWrapping="Wrap"/>
        <TextBlock x:Name="CollectionSummary" Grid.Row="1" Margin="0,8,0,20" Foreground="#666666"/>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="GameList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Tag="{Binding}" Click="Game_Click" Margin="0,0,0,10" Padding="18,14"
                                HorizontalContentAlignment="Stretch" Background="White" BorderBrush="#D4D4D4">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Version}" Margin="0,3,0,0" Foreground="#737373"/>
                                </StackPanel>
                                <TextBlock Grid.Column="1" Text="Open  ›" VerticalAlignment="Center" Margin="16,0,0,0"
                                           Foreground="#7020DF" FontWeight="SemiBold"/>
                            </Grid>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button x:Name="EjectButton" Content="Eject disc" Click="Eject_Click" Padding="18,9" Margin="0,0,8,0"/>
            <Button Content="Close" Click="Close_Click" Padding="18,9"/>
        </StackPanel>
    </Grid>
</Window>
