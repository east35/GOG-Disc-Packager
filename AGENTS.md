# Project workflow

- Whenever a new feature is developed and ready for user testing, publish a test build and place it in a clearly named folder on the user's Desktop before handing the work back.
- Use the repository's `publish.ps1` script for test builds and give the build the feature's intended version when one has been specified.
